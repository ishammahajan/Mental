rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /p2p_messages/{messageId} {
      allow read, write: if true;
    }
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
